/** 应用路由 */
export enum ROUTE {
  HOME = '/',
  LOGIN = '/login',
  PROJECT = '/project',
}

export enum barClassName {
  top = 'appbar-top',
  paddingTop = 'appbar-padding-top',
  marginTop = 'appbar-margin-top',
}
